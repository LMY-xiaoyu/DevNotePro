
# DevNote Pro - 开发者笔记 Pro

**面向程序员的高性能极简笔记应用**

DevNote Pro 是一款专为开发者打造的轻量级笔记工具，结合了 React 的现代化 UI 和 Electron 的原生能力。

---

## 🌟 核心功能

*   **高性能存储**: 采用单文件存储架构 (`Data/Notes/{id}.json`)，极大提升大量笔记场景下的读写性能与数据安全性。
*   **多窗口协作**: 支持将笔记拖拽为独立系统窗口，支持置顶 (Always on Top)，多屏开发利器。
*   **图片支持**: 支持直接粘贴截图/图片到编辑器，自动保存至本地 (`Data/Images/`)。
*   **代码高亮**: 内置 PrismJS，支持 JS, TS, Python, Java, C++, HTML, CSS 等多种语言高亮。
*   **极简设计**: 遵循 Modern Minimalist 设计语言，支持深色/浅色模式自动切换。
*   **双向同步**: 主窗口与浮动窗口数据实时互通，在一处修改，处处更新。

---

## 📂 数据存储结构

应用数据默认存储在 `release` 目录下的 `data` 文件夹（打包后）或项目根目录 `data` 文件夹（开发时）。

*   `Data/Notes/*.json`: 每个笔记单独存储为一个 JSON 文件，文件名即为笔记 ID。
*   `Data/Images/*`: 粘贴的图片资源。
*   `Data/folders.json`: 自定义文件夹结构数据。

> **注意**: 旧版本的 `notes.json` 会在首次启动时自动迁移到新结构，并备份为 `notes.json.bak`。

---

## 🛠️ 构建指南

请确保已安装 **Node.js (LTS)**。

### 1. 安装依赖
```powershell
npm install
```

### 2. 开发模式 (热更新)
```powershell
npm start
```
此命令会自动编译 React 代码并启动 Electron 窗口。

### 3. 打包发布
```powershell
npm run dist
```
打包完成后，可执行文件位于 `release/` 目录下。

---

## ⌨️ 快捷键

*   `Ctrl + N`: 新建笔记
*   `Ctrl + S`: 保存当前笔记
*   `Ctrl + F`: 搜索笔记
*   `Ctrl + V`: 粘贴文本或图片
*   `Esc`: 退出标签添加模式 / 关闭弹窗

---

## 🔧 技术栈

*   **Runtime**: Electron 28
*   **Frontend**: React 18, TypeScript, Tailwind CSS
*   **Build Tool**: Vite 4
*   **Editor**: react-simple-code-editor + PrismJS

License: MIT
